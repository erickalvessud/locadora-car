micronaut:
  server:
    port: 8080

datasources:
  default:
    driverClassName: com.mysql.cj.jdbc.Driver
    dialect: org.hibernate.dialect.MySQL57Dialect
    url: jdbc:mysql://localhost:3306/unisoftsso?autoReconnect=true&useSSL=false
    username: root
    password: mk2lm002

jpa:
  default:
    entity-scan:
      packages: 'com.treino.persistence'
    properties:
      hibernate:
        hbm2ddl:
          auto: validate
        show_sql: false

flyway:
  datasources:
    default:
      enabled: true
      clean-schema: false

grpc:
  server:
    port: 50051
